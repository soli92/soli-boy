---
id: TSK-045
sprint: 6
layer: fe
consumer: agent
priority: P2
estimate: S
status: done
depends_on: [TSK-042, TSK-043]
blocked_by: []
code_path:
  - "packages/app/public/manifest.webmanifest"
  - "packages/app/index.html"
review_status: passed
review_iter: 1
review_report: "code_quality/reports/TSK-045-iter-1.json"
updated: 2026-06-01
---

# TSK-045 — Web app manifest (manifest.webmanifest) + link in index.html (US-038)

## Context

US-038 richiede un web app manifest che abiliti l'installazione come PWA con
nome, icone e colori di brand. Gli asset sono già disponibili in `public/` dopo
TSK-042; `index.html` è già aggiornato con i link favicon da TSK-043. [^src: management/kanban/EP-010-tema-90s-e-identita-di-brand/US-038-web-app-manifest/US-038.md §Business Rules]

## Technical Specs

### File: packages/app/public/manifest.webmanifest

```json
{
  "name": "Soli-boy",
  "short_name": "Soli-boy",
  "description": "Emulatore retro multipiattaforma — web, desktop, mobile.",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0d0d0d",
  "theme_color": "#ff00cc",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}
```

**Note palette:** `background_color: "#0d0d0d"` (fondo scuro brand) e
`theme_color: "#ff00cc"` (accento retro-neon, palette `90s-party` / cyberpunk
di SoliDS). Valori ancorati alla palette documentata nel brand kit — non inventati. [^src: management/kanban/EP-010-tema-90s-e-identita-di-brand/US-038-web-app-manifest/US-038.md §Business Rules]

**Assunzione annotata:** i valori esatti di `background_color` e `theme_color`
sono dedotti dalla palette documentata nel brand kit (fondo scuro + accento neon).
Se l'owner ha valori hex diversi da usare, aggiornare il manifest senza impatto
sugli altri task.

### Link in index.html

Aggiungere nel `<head>`:

```html
<link rel="manifest" href="/manifest.webmanifest" />
<meta name="theme-color" content="#ff00cc" />
```

Il manifest non interferisce con i confezionamenti Electron/Capacitor: viene
semplicemente ignorato nel contesto di shell nativa. [^src: management/kanban/EP-010-tema-90s-e-identita-di-brand/US-038-web-app-manifest/US-038.md §Business Rules]

## Definition of Done

- [x] `packages/app/public/manifest.webmanifest` creato con `name`, `icons` (almeno
      192 e 512), `theme_color`, `background_color`
- [x] `index.html` contiene `<link rel="manifest">` e `<meta name="theme-color">`
- [x] Build produce il manifest nella radice del dist (Vite copia `public/` asis)
- [ ] In Chromium DevTools → Application → Manifest: nessun errore, icone caricate
      (verifica manuale richiesta — out of scope per agent; rinviata a smoke
      manuale post-deploy / qa-dev)
- [x] Gli asset referenziati dal manifest sono quelli copiati in TSK-042 (no duplic.)

## Implementation Notes

- File creati/modificati:
  - `packages/app/public/manifest.webmanifest` (nuovo, 13 righe, JSON valido)
  - `packages/app/index.html` (aggiunti `<link rel="manifest">` + `<meta name="theme-color">`
    nel `<head>` accanto ai favicon di TSK-043, senza toccarli)
- Asset confermati esistenti in `packages/app/public/icons/`:
  `icon-192.png` (7567 B), `icon-512.png` (18833 B) — copiati da TSK-042. Nessun
  asset duplicato creato.
- Verifiche:
  - `python3 -c "import json; json.load(open('packages/app/public/manifest.webmanifest'))"` → OK
    (8 chiavi: background_color, description, display, icons, name, short_name,
    start_url, theme_color; 3 icons entry)
  - `npm run build` (tsc --noEmit + vite build) → OK 2.06s, 61 modules.
    `dist/manifest.webmanifest` presente (545 B), `dist/index.html` contiene
    sia `manifest.webmanifest` sia `theme-color` (grep verificato).
  - `npm run e2e` (Playwright Chromium) → **8/8 verdi** (12.0s); nessuna
    regressione su app.e2e / emulation-real / emulation-gba / emulation-save.
- **Assunzione palette (gap `palette-brand-da-verificare` ancora aperto):**
  `background_color: "#0d0d0d"` e `theme_color: "#ff00cc"` sono dedotti dalla
  palette brand (fondo scuro + accento neon 90s/cyberpunk), **non confermati
  ufficialmente**. L'owner può correggere gli hex sia in
  `manifest.webmanifest` sia nel `<meta name="theme-color">` di `index.html`
  senza impatto strutturale (gli altri TSK di EP-010 non dipendono da questi
  due valori esatti).
- Scope rispettato (R.8): toccati esclusivamente i 2 path dichiarati nel
  frontmatter `code_path`. Nessun touch a be/db/qa/src.
- Nessun commit eseguito (R.14 VCS gate umano).

## Dependencies

[TSK-042, TSK-043]

[^src: management/kanban/EP-010-tema-90s-e-identita-di-brand/US-038-web-app-manifest/US-038.md §Acceptance Criteria]
