---
id: TSK-035
sprint: 5
layer: fe
consumer: agent
priority: P1
estimate: S
status: done
updated: 2026-06-01
depends_on: []
blocked_by: []
code_path: ["packages/app/src/components/Player/**"]
---

# TSK-035 — Schermo intero (Fullscreen API)

## Context
 [^src: management/kanban/EP-005-resa-video/US-020-schermo-intero/US-020.md §Acceptance Criteria]

## Definition of Done
- [x] Funzionalità implementata come da spec — hook `useFullscreen` su
      `packages/app/src/components/Player/useFullscreen.ts` (engine-agnostico,
      agisce sul contenitore `.sb-screen`) e bottone "Schermo intero" nei
      controlli del `Player.tsx`. Toggle invoca `requestFullscreen` sul
      contenitore / `document.exitFullscreen`; uscita Esc gestita via listener
      `fullscreenchange` con cleanup su unmount. Fallback onesto: bottone
      disabilitato + `title` esplicativo se l'API non è disponibile. Accessibilità:
      `aria-label` dinamico (Schermo intero ↔ Esci da schermo intero) e
      `aria-pressed` sincronizzato con lo stato.
- [x] Test (unit/e2e/component) verdi; typecheck OK — nuovo file
      `Player.fullscreen.test.tsx` con 6 casi (rendering bottone,
      requestFullscreen sul contenitore, exitFullscreen al toggle ritorno,
      sync su `fullscreenchange` esterno tipo Esc, fallback API assente,
      cleanup listener su unmount). `npm run typecheck` clean,
      `npm test` 71/71 passati (inclusi i 49 di baseline). e2e Playwright
      fullscreen non aggiunti per scelta (instabilità in headless).
- [x] Nessuna regressione — `Player.test.tsx` (TSK-008/TSK-014) ancora verdi;
      build di produzione OK; scope di scrittura rispettato
      (`packages/app/src/components/Player/**`); nessuna modifica a
      `EmulatorEngine`, scala/aspect (TSK-036) o filtri (TSK-037).

## Dependencies
[]
