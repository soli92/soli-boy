---
id: TSK-120
sprint: 14
layer: be
consumer: agent
priority: P0
estimate: S
status: done
updated: 2026-06-28 15:40
depends_on: []
blocked_by: []
code_path:
  - "packages/app/src/domain/input-mapping.ts"
  - "packages/app/src/domain/**/*.test.ts"
---

# TSK-120 — InputMapping: default keyboard L/R + gamepad indices 4/5

## Context

Aggiungere i pulsanti shoulder L e R al profilo tastiera default e alla mappa gamepad default, e coprire i nuovi mapping con test unitari.
[^src: management/kanban/EP-018-controlli-shoulder-l-r/US-062-tastiera-gamepad-l-r/US-062.md §AC]

## Technical Specs

- **BE:** `packages/app/src/domain/input-mapping.ts`
  - `DEFAULT_KEY_PROFILE`: aggiungere `{ action: 'l', key: 'q' }` e `{ action: 'r', key: 'w' }` (convenzione SNES-like; alternativa ergonomica ammessa se documentata nel commento).
  - `DEFAULT_GAMEPAD_MAP`: aggiungere `{ action: 'l', index: 4 }` e `{ action: 'r', index: 5 }` (Web Gamepad API layout standard).
- **Test:** `packages/app/src/domain/input-mapping.test.ts`
  - Aggiungere casi: `DEFAULT_KEY_PROFILE` contiene L e R; `DEFAULT_GAMEPAD_MAP` contiene indici 4 e 5.
  - Verificare che nessun mapping preesistente sia stato rimosso.

## Implementation Steps

1. Aprire `input-mapping.ts`, individuare `DEFAULT_KEY_PROFILE` e `DEFAULT_GAMEPAD_MAP`.
2. Appendere le entry L e R in entrambe le strutture, rispettando l'ordinamento esistente.
3. Aggiornare `input-mapping.test.ts` con i nuovi casi di asserzione.
4. Eseguire `npm test` (Vitest) dalla cartella `packages/app` e verificare 0 failure.

## Definition of Done

- [ ] `DEFAULT_KEY_PROFILE` contiene le chiavi `l` e `r`.
- [ ] `DEFAULT_GAMEPAD_MAP` contiene gli indici 4 (L) e 5 (R).
- [ ] Test unitari in `input-mapping.test.ts` passano (0 failure, no regressioni).
- [ ] Code review approvata.

## Dependencies

<!-- dipendenze: nessuna (TSK radice della wave) -->
