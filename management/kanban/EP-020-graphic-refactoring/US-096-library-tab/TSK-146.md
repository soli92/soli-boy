---
id: TSK-146
sprint: 18
layer: fe
consumer: agent
priority: P1
estimate: M
status: done
updated: 2026-07-02
depends_on: [TSK-143]
blocked_by: []
code_path:
  - "packages/app/src/components/Library/Library.tsx"
  - "packages/app/src/styles/solids-theme.css"
ui_design_spec: wiki/design/ep020-design-brief.md
---

# TSK-146 — GameTile grid → Card + Badge + Button

## Migrazione

**GameTile `<article className="sd-card sb-tile">`:**
```tsx
<Card className={cn("cursor-pointer transition-shadow hover:shadow-md", isActive && "ring-2 ring-primary ring-offset-2")}>
  <CardContent className="p-0 flex flex-col">
    {/* art placeholder */}
    <div className={`sb-art a-${tile.colorIndex}`} />  {/* mantieni per colori accent */}
    <div className="flex flex-col gap-1 p-3">
      <Button variant="ghost" className="text-left justify-start h-auto p-0 font-semibold text-sm" onClick={onSelect}>
        {rom.title}
      </Button>
      <div className="flex items-center gap-1.5">
        <Badge variant="outline" className="text-xs">{rom.platform}</Badge>
        {isActive && <Badge variant="default" className="text-xs">In gioco</Badge>}
      </div>
    </div>
    <Button variant="destructive" size="sm" className="m-2 mt-0" onClick={onRemove}>
      Rimuovi
    </Button>
  </CardContent>
</Card>
```

**Grid container:**
- `.sb-grid` → Tailwind `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3`

**Mantenere:** classi `.sb-art` + `.a-1..a-5` (accent color ramps — token app-specific non in solids)

**Rimuovere da `solids-theme.css`:** `.sd-card`, `.sb-tile`, `.sb-game`, `.sb-tt`, `.sb-mt`, `.sb-grid` (dopo grep-confirm)

## Pipeline

`/dev TSK-146` → `/visual-oracle TSK-146` → `/a11y TSK-146` → `/ux-ui-review TSK-146` → `/review TSK-146`
