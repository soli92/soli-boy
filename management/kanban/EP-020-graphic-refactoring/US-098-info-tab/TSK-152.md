---
id: TSK-152
sprint: 18
layer: fe
consumer: agent
priority: P2
estimate: S
status: done
updated: 2026-07-02
depends_on: [TSK-143]
blocked_by: []
code_path:
  - "packages/app/src/components/PrivacyNotice/PrivacyNotice.tsx"
  - "packages/app/src/components/StoreComplianceNotice/StoreComplianceNotice.tsx"
  - "packages/app/src/components/LegalNotice.tsx"
  - "packages/app/src/components/UpdateBanner/UpdateBanner.tsx"
ui_design_spec: wiki/design/ep020-design-brief.md
---

# TSK-152 — Info tab notices + UpdateBanner → Card + Alert

## Migrazione

**`PrivacyNotice variant="section"`:**
```tsx
<Card>
  <CardHeader>
    <CardTitle>Privacy</CardTitle>
  </CardHeader>
  <CardContent className="text-sm text-muted-foreground space-y-3">
    {/* contenuto privacy */}
  </CardContent>
</Card>
```

**`PrivacyNotice variant="banner"` (prima-apertura):**
```tsx
<Alert className="fixed bottom-0 inset-x-0 z-[var(--sd-z-toast)] rounded-none border-t">
  <AlertTitle>Informativa sulla privacy</AlertTitle>
  <AlertDescription>{/* testo */}</AlertDescription>
  <Button size="sm" onClick={dismiss}>Ho capito</Button>
</Alert>
```

**`StoreComplianceNotice`:**
```tsx
<Alert variant="destructive">
  <AlertTitle>Avviso legale</AlertTitle>
  <AlertDescription>{/* testo compliance */}</AlertDescription>
</Alert>
```

**`LegalNotice`:**
```tsx
<p className="text-xs text-muted-foreground text-center">{/* testo */}</p>
```

**`UpdateBanner` (Electron, `window.soliboyDesktop` guard invariante):**
```tsx
<Alert className="fixed top-0 inset-x-0 z-[var(--sd-z-toast)] rounded-none">
  <AlertTitle>Aggiornamento disponibile</AlertTitle>
  <AlertDescription>Versione {version} pronta</AlertDescription>
  <div className="flex gap-2 mt-2">
    <Button size="sm" onClick={install}>Installa e riavvia</Button>
    <Button variant="ghost" size="sm" onClick={dismiss}>Più tardi</Button>
  </div>
</Alert>
```

## Pipeline

`/dev TSK-152` → `/visual-oracle TSK-152` → `/a11y TSK-152` → `/ux-ui-review TSK-152` → `/review TSK-152`
