---
id: US-100
title: CSS cleanup + full regression suite
role: developer
priority: P1
status: done
updated: 2026-07-02
blocked_by: []
depends_on: [US-095, US-096, US-097, US-098, US-099]
---

> **Stato:** done (Sprint 17–18, EP-020). Tutti i TSK della US `done` (2026-07-02).

# US-100 — CSS cleanup + full regression suite

## Descrizione

Come developer, voglio rimuovere tutte le classi CSS orfane (`sb-*`/`sd-*` utility) da `solids-theme.css` e `app-extra.css` dopo grep-confirm dell'intero `src/`, e verificare che l'intera test suite (unit + e2e + visual oracle) passi senza regressioni.

## Acceptance Criteria

- [x] Nessuna classe `sb-btn*`, `sb-tog`, `sb-sel`, `sb-range`, `sb-search`, `sb-chip`, `sb-sec`, `sb-lbl`, `sb-row`, `sb-key`, `sb-note`, `sb-grid`, `sb-tile`, `sd-card`, `sd-flex`, `sb-dialog-backdrop` è ancora referenziata in `src/` dopo la rimozione
- [x] Le classi WCAG AA override (`[data-theme="dark"]`, `[data-theme="90s-party"]`, `[data-theme="cyberpunk"]`) sono intatte
- [x] Le classi game-specific invarianti (`.sb-screen`, `.sb-canvas-status`, `.sb-touch-overlay`, positioning rules, safe-area) sono intatte
- [x] `npm run test` ≥ 662/662 unit verdi
- [x] `npm run e2e:ci` 30/30 verdi
- [x] Visual oracle: 3 temi × 2 viewport × 4 tab = 24 screenshot passano
- [x] axe-playwright: 0 critical su tutti i temi
- [x] `npm run build` produce dist valido (nessun Tailwind purge false-negative)

## Task

- [TSK-156](TSK-156.md) — CSS cleanup: rimozione classi orfane
- [TSK-157](TSK-157.md) — Full regression suite finale
