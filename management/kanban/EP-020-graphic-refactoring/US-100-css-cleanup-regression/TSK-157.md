---
id: TSK-157
sprint: 18
layer: qa
consumer: agent
priority: P1
estimate: M
status: todo
updated: 2026-07-02
depends_on: [TSK-156]
blocked_by: []
code_path:
  - "packages/app/"
---

# TSK-157 — Full regression suite finale EP-020

## Checklist di verifica

### Unit tests
```bash
npm run test
```
Atteso: ≥ 662/662 verdi (invariante — nessuna regressione, possibili nuovi test da TSK precedenti)

### E2E tests
```bash
npm run e2e:ci
```
Atteso: 30/30 verdi (incluso `soliboy-theme-switching.acceptance.yaml` con selector aggiornati per TSK-151)

### Visual oracle matrix
```bash
# visual-oracle-protocol: 3 temi × 2 viewport × 4 tab = 24 screenshot
```
- temi: `90s-party`, `dark`, `cyberpunk`
- viewport: mobile 375px, desktop 1280px
- tab: Play (idle + running), Library, Settings (prima sezione aperta), Info
- Atteso: 0 `reject`, ≤1 `warning` tollerato per tema

### A11y scan
```bash
npm run e2e  # con axe-playwright attivo
```
- 0 finding `critical` su tutti i temi e viewport
- Finding `serious` devono avere nota `accepted_reason` (WCAG AA conforme)

### Build check
```bash
npm run build
```
- Nessun errore TypeScript
- Nessun warning Tailwind "content missing" (significa classi non purgabili)
- Bundle size: monitorare che non superi il baseline pre-EP-020 di >20% (Tailwind PurgeCSS riduce la dimensione CSS significativamente)

### Token integrity check
```bash
grep -r "var(--sd-" packages/app/src/styles/ | grep -v "/*" | head -20
```
- Tutte le `var(--sd-*)` reference devono risolvere a token definiti in `@soli92/solids/css/variables.css` o nel `:root` di `solids-theme.css`

## Output

`code_quality/reports/TSK-157-ep020-regression-final.md` — report completo con:
- Esito di ciascuno dei 6 check
- Screenshot matrix summary (24 viewport × tema)
- Bundle size before/after
- Verdict finale: **EP-020 DONE** o lista di blocking issues
