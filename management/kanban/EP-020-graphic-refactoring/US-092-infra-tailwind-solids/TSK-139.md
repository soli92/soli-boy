---
id: TSK-139
sprint: 17
layer: qa
consumer: agent
priority: P0
estimate: S
status: done
updated: 2026-07-02
depends_on: [TSK-138]
blocked_by: []
code_path:
  - "packages/app/"
---

# TSK-139 — Infra smoke test: zero regressioni post-install

## Context

Verificare che l'aggiunta di Tailwind + shadcn bridge + 57 componenti non abbia introdotto regressioni nell'app esistente. Nessun componente è ancora migrato — questa è una verifica puramente additiva.

## Checklist di verifica

1. `npm run build` — passa senza errori Tailwind build o TypeScript
2. `npm run test` — ≥ 662/662 verdi (zero nuovi fallimenti)
3. `npm run e2e:ci` — 30/30 verdi
4. Check layout spot: avviare `npm run dev`, aprire `localhost:5173`, verificare che:
   - Le 4 tab (Play, Library, Settings, Info) renderino identicamente all'attuale
   - Il tema `90s-party` (default) sia attivo
   - Il cambio tema (`dark`, `cyberpunk`) funzioni
   - La Library mostri la griglia ROM senza layout broken
5. Verifica `src/components/ui/` — `ls | wc -l` ≥ 57
6. Verifica `src/lib/utils.ts` — il file esiste e contiene `cn()`

## Output

Report di smoke test in `code_quality/reports/TSK-139-infra-smoke.md` con:
- Esito di ciascuno dei 6 check
- Eventuali warning/deprecation da Tailwind build (non bloccanti se solo warning)
- Conferma che `status: done` è attribuibile a questa TSK
