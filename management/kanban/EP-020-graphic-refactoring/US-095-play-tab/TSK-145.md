---
id: TSK-145
sprint: 18
layer: fe
consumer: agent
priority: P1
estimate: S
status: done
updated: 2026-07-02
depends_on: [TSK-143]
blocked_by: []
code_path:
  - "packages/app/src/components/FileLoader/FileLoader.tsx"
  - "packages/app/src/App.tsx"
  - "packages/app/src/styles/solids-theme.css"
ui_design_spec: wiki/design/ep020-design-brief.md
---

# TSK-145 — FileLoader + idle CTA → solids Button

## Migrazione

**FileLoader CTA (`<label className="sb-btn sb-btn-primary">Carica ROM</label>`):**
- `<Button asChild variant="default"><label htmlFor="file-input">Carica ROM</label></Button>`
- `<input id="file-input" type="file" className="sr-only" ...>` (invariante)
- `Button asChild` usa Radix Slot per applicare i Button styles al `<label>` senza wrapper HTML extra

**Drop zone (`.sb-dropzone`):**
```tsx
<div className="flex-1 min-h-16 flex items-center justify-center border-2 border-dashed border-border rounded-lg text-sm text-muted-foreground cursor-pointer focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring">
  Trascina qui le ROM...
</div>
```

**Idle CTA in Play tab (`.sb-play-idle-cta`):**
```tsx
<div className="flex flex-col items-center gap-4 p-8 text-center">
  <p className="text-muted-foreground text-sm">Nessun gioco in esecuzione</p>
  <Button variant="outline">Vai alla Libreria</Button>
</div>
```

**Rimuovere da `solids-theme.css`:** `.sb-loader`, `.sb-dropzone`

## Pipeline

`/dev TSK-145` → `/visual-oracle TSK-145` → `/a11y TSK-145` → `/ux-ui-review TSK-145` → `/review TSK-145`
